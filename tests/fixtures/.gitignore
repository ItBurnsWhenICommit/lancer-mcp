# ============================================================================
# Test Fixtures .gitignore
# ============================================================================
# Fixtures are generated locally using scripts/refresh-fixtures.sh
# They are NOT committed to the repository due to their size
# ============================================================================

# Ignore all database dump files (large binary files)
dumps/*.dump

# Ignore metadata files (can be regenerated)
dumps/*.metadata.json

# Keep the directory structure
!dumps/.gitkeep

# Ignore all Git mirror repositories (large binary data)
repos/*
!repos/.gitkeep

# ============================================================================
# Optional: Commit specific baseline fixtures
# ============================================================================
# Uncomment to commit a specific baseline dump for CI/CD:
# !dumps/lancer_baseline.dump
# !dumps/lancer_baseline.metadata.json
