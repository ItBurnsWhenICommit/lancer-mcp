# Ignore large dump files (commit only the latest or specific versions)
dumps/*.dump

# Keep metadata files
!dumps/*.metadata.json

# Keep the repos directory structure but ignore the actual Git data
# (Users should run refresh-fixtures.sh to populate)
repos/*
!repos/.gitkeep

# Allow specific committed fixtures if needed
# Uncomment to commit a specific dump:
# !dumps/project_indexer_baseline.dump

